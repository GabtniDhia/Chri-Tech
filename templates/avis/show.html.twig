{% extends 'base.html.twig' %}

{% block title %}Avis{% endblock %}

{% block content %}
    <h1>Avis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avi.id }}</td>
            </tr>
            <tr>
                <th>Etat_Service</th>
                <td>{{ avi.EtatService }}</td>
            </tr>
            <tr>
                <th>Recommendation</th>
                <td>{{ avi.Recommendation }}</td>
            </tr>
            <tr>
                <th>DescriptionService</th>
                <td>{{ avi.DescriptionService }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('avis_index') }}">back to list</a>

    <a href="{{ path('avis_edit', {'id_avis': avi.id}) }}">edit</a>

    {{ include('avis/_delete_form.html.twig') }}
{% endblock %}
