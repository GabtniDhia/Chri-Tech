{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block content %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom_Cat</th>
                <td>{{ categorie.NomCat }}</td>
            </tr>
            <tr>
                <th>Type_Cat</th>
                <td>{{ categorie.TypeCat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_index') }}">Retour au liste</a>

    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}">Modifier</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
