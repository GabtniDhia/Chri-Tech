{% form_theme form 'bootstrap_4_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_row(form.q) }}
<h4 style="color: #2aabd2">Organisateur</h4>
{{ form_row(form.categorie) }}
<h4 style="color: #2aabd2">Prix</h4>
<div class="row">
    <div class="col-md-6">
        {{ form_row(form.min) }}
    </div>
    <div class="col-md-6">
        {{ form_row(form.max) }}
    </div>
</div>
<h4 style="color: #2aabd2">Promotions</h4>
{{ form_row(form.promo) }}
<button type="submit" class="btn btn-primary w-100">filtrer</button>
{{ form_end(form) }}