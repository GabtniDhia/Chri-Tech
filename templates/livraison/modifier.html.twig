{% extends 'baseback.html.twig' %}
{% block title %} Espace Admin {% endblock %}
{% block titre1 %} Gestion de livraison{% endblock %}


{% block content %}

    {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}

    <div class="checkout-section mt-150 mb-150">
        <div class="container">
            <div class="row">
                <div class="col-lg-8">
                    <div class="checkout-accordion-wrap">
                        <div class="accordion" id="accordionExample">
                            <div class="card single-accordion">
                                <div class="card-header" id="headingOne">
                                    <h5 class="mb-0">
                                        <button class="btn btn-link" type="button" data-toggle="collapse" data-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                                            Modifier la livraison
                                        </button>
                                    </h5>
                                </div>

                                <div id="collapseOne" class="collapse show" aria-labelledby="headingOne" data-parent="#accordionExample">
                                    <div class="card-body">
                                        <div class="billing-address-form">

                                            <p>{{ form_errors(form.adresse) }}</p>
                                            <p>{{ form_widget(form.adresse) }}</p>
                                            <p>{{ form_errors(form.codepostal) }}</p>
                                            <p>{{ form_widget(form.codepostal) }}</p>
                                            <p>{{ form_errors(form.ville) }}</p>
                                            <p>{{ form_widget(form.ville) }}</p>
                                            <p>{{ form_errors(form.datelivraison) }}</p>
                                            <p>{{ form_widget(form.datelivraison) }}</p>
                                            <p>{{ form_widget(form.commande) }}</p>
                                            <p>{{ form_errors(form.commande) }}</p>
                                            {{ include('livraison/_form.html.twig', {'button_label': 'Modifier'}) }}
                                            {{ include('livraison/supp.html.twig' , {'button_label': 'Annuler'}) }}
                                            <a href="{{ path('livraison_index', {'id': livraison.id}) }}">Voir la livraison</a>

                                        </div>
                                    </div>
                                </div>
                            </div>


                        </div>
                    </div>

                </div>
            </div>


        </div>
    </div>
    </div>
    {{ form_end(form) }}
{% endblock %}
